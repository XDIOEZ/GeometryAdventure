**添加规则文件可帮助模型精准理解你的编码偏好，如框架、代码风格等**
**规则文件只对当前工程生效，单文件限制10000字符。如果无需将该文件提交到远程 Git 仓库，请将其添加到 .gitignore**

字段使用Unity的Tooltip属性

方法使用XML注释 {
    /// <summary>
    /// 只要这样就行不用过于复杂
    /// </summary>
}

习惯使用Region预处理指令分割代码记得使用中文

脚本尽量遵循 字段->属性->Unity生命周期->其他函数方法 的结构